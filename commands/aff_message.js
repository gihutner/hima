const Discord = require('discord.js');

module.exports = {
    name: 'affiliate',
    description: '',
    execute(message, args) {
        if (message.guild.members.cache.get(message.author.id).has('744625481592340600')){
        }
    },
};